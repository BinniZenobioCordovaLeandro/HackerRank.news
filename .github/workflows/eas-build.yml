name: EAS Build App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: yarn

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: o_YlM3D47YGN2NChi-VQssSWIMCMoN3wT0m0rZao # TODO: replace with GITHUB SECRET, TEMPORAL_HACKERRANK_NEWS_APP - this is a temporary token on my BinniCordova Account
          # token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: yarn install

      - name: 🚀 Build app
        run: eas build --platform android --non-interactive